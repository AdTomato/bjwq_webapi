<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.authine.cloudpivot.web.api.mapper.ClientMapper">
    <!-- 根据客户名称获取一级客户中的业务员 -->
    <select id="getFirstLevelClientSalesman" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT salesman FROM id34a_first_level_client WHERE company_name = #{clientName} ORDER BY createdTime DESC
    </select>

    <!-- 根据客户名称获取二级客户中的业务员 -->
    <select id="getSecondLevelClientSalesman" parameterType="java.lang.String" resultType="java.lang.String">
        select salesman FROM id34a_second_level_client WHERE company_name = #{clientName} ORDER BY createdTime DESC
    </select>

</mapper>