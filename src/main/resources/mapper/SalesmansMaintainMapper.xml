<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authine.cloudpivot.web.api.mapper.SalesmansMaintainMapper">
    <select id="getSalesmansAndDepartment" resultType="com.authine.cloudpivot.web.api.entity.SalesmansMaintain">
        SELECT
          salesman, department
        FROM
          ibkct_salesmans_maintain
        WHERE
          first_level_client_name = #{firstLevelClientName}
          AND
          second_level_client_name = #{secondLevelClientName}
          AND
          business_type = #{businessType}
          AND
          welfare_operator = #{welfareOperator}
    </select>
</mapper>